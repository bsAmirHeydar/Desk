# R1 Tests

Run:

```powershell
python "RUNTIME/R1 Foundation/tools/alpha_runtime.py" --vault-root ".\Institutional_Fundamental_Macro_Research_OS_v5_1_FINAL" selftest
```

The suite uses an isolated temporary data root and never writes research data into the Vault.

# 04 Canonical Run Contract

Current Alpha Desk scope is Gold only. `Run` means `Run Gold`. Local `run Gold` follows the promotion state and remains V2 while V3 is shadow. `commission Gold` explicitly runs V3 shadow. P05 does not implement P10 one-run production orchestration.

# 02 Freeze Semantics and Historical Baselines

Historical hash manifests are evidence, not moving targets. P01 keeps its original V2 snapshot byte-for-byte. Current freeze validation exempts only explicitly deployment-owned paths while preserving fail-closed protection for immutable legacy science. P02 similarly keeps its original snapshot in P05 baseline while the current acquisition freeze excludes derived metadata.

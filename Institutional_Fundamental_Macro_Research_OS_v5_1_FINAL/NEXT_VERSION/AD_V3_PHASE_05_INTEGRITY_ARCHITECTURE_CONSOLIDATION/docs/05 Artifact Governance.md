# 05 Artifact Governance

Runtime receipts, acquisition runs, semantic packets, causal runs, commissioning outputs, HTML reports, true-forward state, promotion state, capsules, and Python caches are non-authoritative runtime artifacts. Canonical configs remain committed and are never hidden by blanket ignore rules.

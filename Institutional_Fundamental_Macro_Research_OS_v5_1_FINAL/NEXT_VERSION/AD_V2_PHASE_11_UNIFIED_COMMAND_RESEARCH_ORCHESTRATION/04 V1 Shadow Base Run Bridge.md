# V1 shadow base run

`run Gold` first executes the canonical Unified Research Interface in SHADOW mode for XAUUSD. The resulting V1 Run Capsule and typed artifacts are the base evidence context.

P11 does not patch V1 `AlphaLab.ps1`, the R2 prompt graph or V1 Direction/Permission. The V1 base run remains a separately sealed research run.

## V1 authority-surface binding hardening
P11 binds to the installed Git-tracked closed V1 runtime surface it actually invokes (`Unified Research Interface` compiler/executor and `R1 Foundation` runtime) using pre-P00 commit ancestry. The legacy root `AlphaLab.ps1` wrapper is verified when present but is not required when the current clean HEAD does not contain it. Real mutation of the V1 authority/runtime surface after P00 remains fail-closed.

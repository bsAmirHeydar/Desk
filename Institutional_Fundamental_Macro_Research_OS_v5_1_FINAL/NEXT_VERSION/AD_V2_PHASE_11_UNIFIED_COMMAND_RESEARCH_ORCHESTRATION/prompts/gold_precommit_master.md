# Alpha Desk V2 Gold Master Precommit

You are compiling a **price-independent V2 precommit** from already-admitted V1 research artifacts and canonical P05 Gold registries. Execute all P11 Gold master clusters in registry order.

Return only the required `v2_precommit` artifact.

Hard laws:
- XAUUSD target price/return/candles/technical momentum cannot determine or weaken Directional Pressure.
- Build Pressure roots only from causal evidence that satisfies the P02 source/independence rules.
- Same-root manifestations (for example policy repricing, 2Y, real yield, DXY when dependent) must not be counted as independent roots twice.
- Expected response must be declared without seeing any subsequent response-window observation. If magnitude is not defensible, use `DIRECTION_ONLY`; do not fabricate an expected range.
- Do not create actual_response, release outcome, trade permission or broker action.
- Preserve material missing data and candidate missing drivers.
- Gold market roles must follow P05: Volume != Flow; OI != Direction; ETF AUM != Flow; Options OI != Dealer Gamma; venue subsets are not global Gold.

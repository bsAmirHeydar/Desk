# P12 handoff

After P11, the only fixed-roadmap task is P12 final implementation freeze and acceptance. P12 fingerprints the unified launcher/cluster and the complete P00–P11 stack. After P12, new phases require evidence-driven scientific need.

## V1 authority-surface binding hardening
The installed Git-tracked V1 authority/runtime surface used by the direct shadow bridge is bound by semantic contract and pre-P00 commit ancestry. `AlphaLab.ps1` is optional when absent from the current clean HEAD; when present it is verified but never patched by P11. Real V1 runtime mutation after P00 remains fail-closed.

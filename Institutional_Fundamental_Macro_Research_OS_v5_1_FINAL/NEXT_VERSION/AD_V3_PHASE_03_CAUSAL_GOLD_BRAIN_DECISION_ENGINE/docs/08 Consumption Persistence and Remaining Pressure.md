# Lifecycle state

Consumption is an eight-component vector, not a percentage. Persistence is driver-specific and separate from freshness. Remaining pressure is a qualitative lifecycle conclusion derived from active roots, repricing, realized flow, contradiction and persistence; price distance alone is forbidden.

Runtime now emits the eight-component vector, per-root persistence stack, and qualitative remaining-pressure state.

# P04 handoff

P04 must integrate the P03 receipt into the Control Room, add true-forward precommit/response comparison, persist immutable run capsules, calibrate qualitative states, test missing-driver escalation and only then consider V3 production promotion.

# Synthetic E2E

The synthetic Gold case proves wiring and invariants only. It is always `DEVELOPMENT_CASE` and cannot enter P07 TRUE_FORWARD counts or P09 recurrence gates.

A synthetic PASS proves integration correctness, not market edge.
# Commissioning gates

P10 RC commissioning requires exact dependency fingerprints, P10 validation, P09 regression, integrated synthetic Gold pipeline, R4 CORE, clean authority boundaries and a writable external data root.

Optional `FULL` certification can be requested explicitly. Failure of empirical-maturity gates does not fail RC engineering commissioning; it is reported as `EVIDENCE_PENDING`.
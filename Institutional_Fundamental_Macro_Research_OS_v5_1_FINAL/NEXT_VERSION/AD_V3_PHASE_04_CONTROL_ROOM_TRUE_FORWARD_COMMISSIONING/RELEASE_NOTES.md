# AD-V3-P04 — Control Room, True-Forward Commissioning, and Promotion

P04 does not add a second scientific brain. P01–P03 remain frozen. P04 orchestrates their outputs, applies a governed semantic boundary, renders human operational intelligence, precommits true-forward expectations, and owns the final promotion gate.

Initial deployment is **SHADOW_COMMISSIONING**. `AlphaDesk.ps1 run Gold` remains routed to the frozen V2 baseline until P04 is explicitly promoted after sufficient true-forward evidence. V3 commissioning is invoked with `AlphaDesk.ps1 commission Gold`.

Hard invariants: Pressure is not Price; Direction is not Edge; Edge is not Permission; UNKNOWN is preserved; proxy evidence cannot be inflated; no automatic production promotion; no trade execution authority.

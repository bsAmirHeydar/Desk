# Model Bridge Contract

P06 uses the existing R3 MODEL_PROCESS host boundary. It can auto-bind the local commissioned OpenAI host only when environment credentials are present. Secrets remain environment-only.

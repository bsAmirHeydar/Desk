# Semantic Failure Policy

Transport, schema, identity, authority, evidence, or guard failures degrade the affected request to UNKNOWN/non-additive and are recorded as fallback/rejection. No retry searches for a preferred direction.

# Operator handoff

After P12, routine human use is `AlphaDesk.ps1 run Gold`. Operational inspection uses `status`, `health`, `tf-status`, `learning-status`, `rc-status`, and P12 `final-status`. No knowledge of individual P02–P11 commands is required for normal use.
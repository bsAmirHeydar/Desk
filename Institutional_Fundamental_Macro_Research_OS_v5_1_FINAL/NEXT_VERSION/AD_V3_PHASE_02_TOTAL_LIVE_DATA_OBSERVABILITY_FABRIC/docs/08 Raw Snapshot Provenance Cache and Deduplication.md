# Provenance

Every successful fetch can persist its raw body under a source namespace using retrieval time + SHA-256. A source shared by many facts is fetched once per run. Observations retain source ID, provenance reference and raw hash.

# Demotion, Retirement and Revalidation

Every promoted rule has a retirement trigger and revalidation cadence.

Possible actions:
- keep `PROMOTED`;
- narrow scope;
- downgrade authority class;
- suspend;
- return to shadow;
- retire.

Retirement never deletes historical performance. A new materially changed rule receives a new `modifier_version` and starts a new validation lineage.

# Live Learning Data
The ledger and JSONL archive are seeded from the existing Alpha Lab persistent forward-learning store as of patch build time. They are point-in-time research records, not static rules. Future runs append new records; prior archive states remain immutable. Learning status promotion is recorded separately rather than rewriting historical observations.

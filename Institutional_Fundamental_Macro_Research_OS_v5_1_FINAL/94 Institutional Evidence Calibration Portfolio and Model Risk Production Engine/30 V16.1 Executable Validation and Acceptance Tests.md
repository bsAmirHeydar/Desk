# V16.1 Executable Validation and Acceptance Tests

The release includes executable cases for:
- Fundamental sole direction authority;
- emerging-aligned Narrative still allowing Active with cap;
- weak Fundamental never upgraded by Narrative;
- neutral/conflicted/opposing Narrative preventing Active without creating opposite direction;
- secondary evidence uncertainty capping rather than blocking;
- decision-critical unknown dependency producing HOLD;
- Timing contextual/secondary uncertainty not over-vetoing;
- missing clock drift blocking operational ingest;
- closed market blocking new entry;
- invalid permission blocking;
- stale quote / bad ATR / duplicate order / position desync blocking at entry-trigger stage;
- cohort-key separation by edge/evidence/execution profile;
- train vs holdout metrics separated;
- direction-aware portfolio netting;
- legacy retrieval denial;
- manifest mirror drift detection;
- runtime vs deployment preflight separation;
- event-fast-path frozen-pack integrity;
- strict state enum/date validation;
- immutable outcome append behavior.

Run `tools/alphalab_selftest.py` and deployment preflight before release.

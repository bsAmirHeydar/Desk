# ALPHA LAB — RUN COMMAND

## The only command the user needs

After uploading this complete Vault to ChatGPT, the normal live-research command is simply:

```text
run NASDAQ100
```

or:

```text
run XAUUSD
```

When the latest user message matches `run <subject>`, treat it as an explicit instruction to execute the canonical Alpha Lab Chat-Native Run Contract in:

`Institutional_Fundamental_Macro_Research_OS_v5_1_FINAL/RUNTIME/Unified Research Interface/04 Chat-Native Run Contract.md`

The user does **not** need to specify mode, horizon, depth, research classes, prompt IDs, clusters, sources, M1, APL-A, or report format. Those are compiled automatically.

### Default semantics

`run <subject>` means:

- live/current research (`MODE = LIVE`);
- `AS_OF = NOW`;
- current-session / day-trading focus with daily carry context;
- maximum scientifically useful depth internally;
- all relevant Alpha Lab research classes selected automatically;
- the eight canonical analytical clusters executed as needed;
- M1 method governance applied;
- APL-A applied separately as shadow perspective only;
- current external evidence retrieved when needed;
- final output in Persian using UI2 three-layer reporting;
- Layer 1: instant answer;
- Layer 2: complete simple overview;
- Layer 3: full section-specific deep dives;
- next review time/condition included when scientifically available.

### Chat-native execution

For Chat use, do **not** require local Windows runtime, `OPENAI_API_KEY`, C1 environment certification, Task Scheduler, or broker connectivity. The attached Vault defines **how to think**; ChatGPT/web retrieval provides current external evidence for LIVE research.

### Scientific invariants

Never weaken the Vault's current rules: no lookahead, no fake precision, UNKNOWN remains UNKNOWN, narrative is not fact, APL-A does not set Direction, and contradictory evidence remains visible.

### Unsupported subjects

Resolve aliases through the Vault. If the subject is outside the certified runtime universe but is researchable by the Vault, run it as `RESEARCH_SUPPORTED_UNCERTIFIED / NEW_ASSET_RESEARCH` and clearly label that status. Do not falsely call it production-certified.

### No follow-up friction

Do not ask the user for extra fields that can be inferred safely. If the symbol is genuinely ambiguous, resolve using the Vault's subject resolver or state the ambiguity briefly and use the most defensible canonical interpretation.
